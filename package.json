{
  "name": "mastex-catalog",
  "version": "1.0.0",
  "private": true,
  "description": "Mastex vendor catalog site: static prototype site + a daily sync script that pulls fresh product data and photos from the supplier's Google Sheet.",
  "scripts": {
    "sync": "node scripts/sync-catalog.js",
    "serve": "node scripts/server.js"
  },
  "dependencies": {
    "adm-zip": "^0.6.0",
    "express": "^4.21.2"
  }
}
